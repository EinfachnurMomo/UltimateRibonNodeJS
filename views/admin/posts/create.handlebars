<div class="container">
    <h1>Neue News erstellen</h1>

    <div class="row p-4">
        <div class="col-md-10">
            <form action="/admin/posts/create" method="post" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="title">Titel</label>
                    <input type="text" class="form-control" name="title" id="title" placeholder="Gebe ein Titel an">
                </div>

                <div class="form-group">
                    <label for="file">Datei hochladen</label>
                    <input type="file" class="form-control" id="file" name="uploadedFile"  accept="image/jpeg, image/jpg, image/png, image/bmp">
                </div>

                <div class="form-group">
                    <label for="status">Status</label>
                    <select name="status" id="status" class="form-control">
                        <option value="public">Öffentlich</option>
                        <option value="private">Privat</option>
                        <option value="draft">Entwurf</option>
                    </select>
                </div>

                <!--<div class="form-group">
                    <label for="category">Kategorie</label>
                    <select name="category" id="category" class="form-control">
                        {{#each categories}}
                            <option value="{{_id}}">{{title}}</option>    
                        {{/each}}                                          
                    </select>
                </div>-->

                <div class="form-check ml-3">
                    <label for="allowComments">
                        <input type="checkbox" name="allowComments" id="allowComments" class="form-check-input">
                        Kommentare erlauben
                    </label>
                </div>

                <div class="form-group">
                    <label for="description">Inhalt</label>
                    <textarea name="description" id="description" class="form-control" placeholder="Gebe hier den Inhalt an" rows="10"></textarea>
                </div>

                <button class="btn btn-outline-success btn-lg" type="submit">News erstellen</button>
            </form>
        </div>
    </div>
</div>